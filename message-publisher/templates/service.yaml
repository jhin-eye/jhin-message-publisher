apiVersion: v1
kind: Service
metadata:
  name: {{ include "message-publisher.fullname" . }}
  labels:
    {{- include "message-publisher.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "message-publisher.selectorLabels" . | nindent 4 }}
